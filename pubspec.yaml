name: buffer_list
description: BufferList JS implementation in Dart.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.1

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  magic_buffer_copy:
    path: c:/users/kareemzaher/desktop/darTedious/magic_buffer_copy

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
